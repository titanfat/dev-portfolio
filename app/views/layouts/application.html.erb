<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title><%= @page_title %></title>
    <meta name="keywords" content="<%= @seo_keywords %>"/>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Caveat+Brush&family=Open+Sans&family=Roboto:ital,wght@0,400;1,300&display=swap" rel="stylesheet">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= favicon_link_tag 'favicon/favicon.ico' %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
   <div class="site-wrapper">
      <div class="site-wrapper-inner">
        <div class="cover-container">
          <div id="background"></div>
          <%= video_tag(
                  'videoplayback_Trim.mp4',
                  id: 'background',
                  autoplay: true,
                  loop: true,
                  muted: true,
                  poster: 'videoplayback.jpg'
              )
          %>
          <%= render 'shared/application_nav' %>
          <%= source_helper('application') %>
          <%= alerts %>
          <%= yield %>
          <%= render 'shared/application_footer' %>
        </div>
      </div>
   </div>
  </body>
</html>
